# ElectronJS + NestJS Microservices

Node version.

```
v18.16.0
```

Install all dependencies with yarn.

```bash
yarn
```

To run it without packing it.

```bash
yarn start
```

To build and pack it using electron-builder.

```bash
yarn distribution
```
